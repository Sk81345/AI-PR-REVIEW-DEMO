name: 🤖 AI Pull Request Review (PowerShell + RAG)

on:
  pull_request:
    branches:
    - main
    - master
  # 👇 CORRECTED: NO leading spaces here
  workflow_dispatch:


jobs: # ... (rest of the file is correct)
